打开kinect颜色数据并显示，使用qml显示
设置故障判断
显示两个kinect图像color+depth
加入故障判断和数据格式设置，还有颜色深度对准

加入imagevalue求值，并显示在右侧
(针对深度0点：显示照常显示，但深度[min,max]映射到 [0,255]时min通常是深度0点，这样显示的就不好，例如本来将[500,1000]映射到[0,255],由于0点噪点的存在，相当于把[0,1000]映射到l[0,255]，
所以求min时需要mask掉深度0点，
以mask掉
在imagevalue中计算深度均值时已经将深度0点舍去了

考虑联合双边滤波算法